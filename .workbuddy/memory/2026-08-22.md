# 2026-08-22 工作日志

## 核对用户已完成的 BUG_FINDINGS 修复（全量验证通过）

用户按 BUG_FINDINGS.md 自行完成修复，本轮逐项核对 + 回归验证，**全部确认**：

### 用户修复确认清单（16 项）
- **P0-1** TrayIcon.ps1 全部 7 处 `.GetNewClosure()` 移除、`$using:` 全改 `$script:`（计数验证：GetNewClosure=0 / $using:=0）
- **P0-2** 点击异常挂死：`Application.SetUnhandledExceptionMode(CatchException)` + `add_ThreadException` 兜底（隐藏模态框不再出现）+ 轮询 DoEvents 包 try/catch
- **隐藏 bug 顺带修复**：Start-Tray 新增 `$script:cliPath=$cliPath`（用解析后值而非空 param）+ `$script:StatePath=$StatePath`（此前从未赋值，Open Center 点击为 null）
- **P1-13** Get-TimeAgo 24h+：`$days=[math]::Floor($hours/24)` 先算再插值（边界 oracle 0s~7d 全过，24h→`1d ago`）
- **P1-1/P1-2** win32.ts Toast：`actions:['View','Dismiss']` 字符串数组 + 回调按 `view/activate→Center`、`dismiss→mark-all-read`（node-notifier toaster.js 实读确认：点按钮回传 buttonClicked;button=label，正文=activate）
- **P1-3/4** win32.ts 路径：getDaemonCliPath `../daemon-cli.js`、getCenterPath `../../src/center/CenterWindow.ps1`（node 实测 + win32-paths.test.ts）
- **P1-5** daemon.ts cliPath `path.join(__dirname,'daemon-cli.js')`（与旧式等价）
- **P1-6** dedup 磁盘持久化 `~/.agent-attention/dedup.json` + AGENT_ATTENTION_HOME + 内存快路径
- **P1-7** readState 仅 unreadCount/visible 变化时重写（打破 chokidar 死循环）
- **P1-8** daemon 自持 daemon.lock：`openSync('wx')` 原子获取 + stale steal（pid 死亡偷锁）+ SIGTERM/SIGINT/uncaughtException/beforeExit 全释放；CLI start 只清 stale 不持锁
- **P2-1** daemon-cli `$_ .name` 空格 → `$_.name`（PS 解析错误）
- **P2-2** markAgentEventsRead `visible: newUnreadCount>0`（原来 events.length>0 永不隐藏）
- **P3-7** pushStateToTrayFile 开头 `if(stopped) return`（stop 后迟到 push 重建 tray-state.json 的坑）
- **P1-9** CenterWindow WPF `$s.Parent.Children.Remove($s)` 替代不存在的 RemoveChild
- **P1-9 伴** per-event 已读按钮 `$nodePath` 环境变量 fallback
- 新增回归测试：daemon-lock.test.ts（P1-8/P3-7/P2-1）、win32-paths.test.ts（P1-3/4/1/2，含 stripComments + dist 编译产物检查）

### 本轮我追加的 2 处修复
1. **两个 ps1 补 UTF-8 BOM**（TrayIcon.ps1 / CenterWindow.ps1）：无 BOM 时 PS 5.1 `-File`/ParseFile 按 ANSI(GBK) 解码 → `●`/`✓`/`·`/`…` 等非 ASCII 显示乱码 + ParseFile 假阳性解析错误。用 .NET 二进制前置 EF BB BF，内容零改动。
2. **registry.ts + registry.test.ts 测试隔离**（P0 测试卫生）：原测试 `withTempRegistry` 注释声称重定向 temp 但实际没做（REGISTRY_PATH 是模块常量），全部写到真实 `~/.agent-attention`，afterEach/finally 又 `fs.rmSync(recursive)` 递归删真实目录 → ①沙箱累计删除 ≥50 触发 SAFE_DELETE_BULK_CONFIRM_REQUIRED 守卫误报（全量跑 registry suite 14 用例全失败，单独跑 14/14 过 = 实锤误报）②真实环境跑 jest 会删用户 daemon 状态。修复：registry.ts 引入 `getRegistryPath()`（AGENT_ATTENTION_HOME 注入，与 dedup 同约定）；测试 beforeEach 建 temp home + afterEach 只删 temp。

### 验证证据
- `npm run build`：tsc 零错误
- `npm test`：**10 suites / 86 tests 全过**（此前全量 14 失败 → 修复 registry 隔离后 0 失败）
- Get-TimeAgo 边界 oracle：0s→0s ago / 59s→59s ago / 60s→1m ago / 59m→59m ago / 60m→1h ago / 23h59m→23h ago / 24h→1d ago / 48h→2d ago / 7d→7d ago
- 两 ps1 ParseFile（BOM 后默认路径）：PARSE OK

### 遗留（未修，非本轮回归）
- F1 jumpToTarget 死代码（v0.3 若承诺"点击回 Agent"为验收 blocker，接真实调用或撤承诺）
- CenterWindow 保留 5 处 `.GetNewClosure()` **正确**（for 循环内捕获 $evId/$evCliPath 局部变量，不依赖 $script: 写回，与 TrayIcon 场景本质不同）
- daemon 无 watchdog（daemon 自身 crash 无人拉起）——架构决策，按用户要求不建 RESTARTING 状态
- doctor Sound 检查 `process.platform==='win32'` 假阳性（P3 级）

## .NET 10 工具链落地（支撑 PowerShell→C#/.NET 迁移）

- 经 `mise install dotnet@10` 安装 .NET 10 SDK 10.0.400；安装路径 `C:\Users\lenovo\AppData\Local\mise\dotnet-root`（mise 逻辑路径 `installs\dotnet\10.0.400`）。
- 首次安装 mise 自检误报失败：其收尾 `dotnet --list-sdks` 调到了系统 7.0 宿主（`C:\Program Files\dotnet`），看不到 mise 装的 10.0.400。重跑一次并预先把 dotnet-root 顶到 PATH 最前 → 自检解析到 10.0.400、`.NET 10.0.400 is already installed`、最终 `✓ installed`，未重下 300MB。
- 运行时含 `Microsoft.WindowsDesktop.App 10.0.11`（WPF+WinForms 包在 `packs/`），即能编托盘(Center 用 WPF)与 Tray(WinForms)。
- 真 WPF 冒烟：`dotnet new wpf` + `dotnet build -c Release` → 0 警告 0 错误，产出 `net10.0-windows` exe。迁移目标栈实证可行。
- 注意：裸 `dotnet` 仍解析到系统 `C:\Program Files\dotnet`（user PATH 排在 system 之后，预置无效）；用户真实 shell 靠 `mise activate` 的 shim 前置才指向 10.0.400。本仓构建 WPF 时用完整路径 `C:\Users\lenovo\AppData\Local\mise\dotnet-root\dotnet.exe` 最稳。

## mise 接管 + 迁移设计文档

- 建 `mise.toml` pin `dotnet = "10.0.400"`（项目级接管，不动 Node 工具链）；`mise where dotnet` / `mise which dotnet` 均正确解析到 10.0.400。
- **mise 2026.8.5 已知 bug（本沙箱实测）**：`mise hook-env` / `mise exec dotnet -- ...` 会 panic（tokio 运行时崩溃 "Async Backtrace"），与 dotnet 无关——`mise where`/`mise which` 正常。GitHub API 403 挡了 `mise self-update`，release asset CDN 命名亦未命中，升级通道在本环境不通。用户真实交互终端通常无碍；若 `mise activate` 也崩，用 `scoop update mise` 升 2026.8.10。保底：直接调 `C:\Users\lenovo\AppData\Local\mise\dotnet-root\dotnet.exe`。
- 产出迁移设计文档 `docs/migration-powershell-to-dotnet.md`：冻结现有架构（文件即真相/daemon 单例/CLI 独立/State-Registry bounded context 不变），Tray 改 WinForms、Center 改 WPF，daemon→UI 进程边界契约保留，新增 Windows Named Pipe 实时通道（协议契约：管道命名/帧格式/5 类消息/ACL/重连/回执），冻结清单（state.json/tray-state.json 兼容边界 + spawn 参数名沿用），分阶段 P0–P4 路线 + 回滚 + L3 Oracle 映射。
- 关键源码锚点（文档已对位）：`src/center/TrayIcon.ps1`、`src/center/CenterWindow.ps1`（待重写）；`win32.ts:76-80` Toast View spawn、`win32.ts:89` Dismiss spawn node mark-all-read；`daemon.ts:83-100` pushStateToTrayFile（light hash 仅真变回写）、`daemon.ts:129-137` spawnTray 传 -TrayStatePath/-CliPath/-TrayPidPath、`daemon.ts:170` chokidar.watch、`daemon.ts:261` O_EXCL 锁。
