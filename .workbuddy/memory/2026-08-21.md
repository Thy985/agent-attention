# 2026-08-21 工作日志

## Code Review：agent-attention (v0.2)

应请求对 `agent-attention` 已实现的代码/功能做了带证据的严厉审查。核心发现（均已在本地复现）：

1. **P0 紧急声音路径是死代码** — `src/notification/win32.ts:61` 与 `src/jump.ts:40` 用 `spawn(process.execPath, ['powershell', ...])`，但 `process.execPath` 是 **Node 二进制**，于是 node 把 'powershell' 当成模块加载 → exit 1。已用 `node _proof.js` 复现（exit code 1, node 模块加载器报错）。后果：`permission_required`(P0) 的"更响提示音"与"点击跳回 Terminal"功能从未真正运行。
2. **所有优先级声音无区分** — win32.ts 对每个事件传入相同 `sound: soundEnabled`，node-notifier 在 Windows 上走 snoretoast `-s Bottle`（已核对 node_modules/node-notifier/notifiers/toaster.js + lib/utils.js）。P0 与 P2 声音完全相同，违反 PRD §5.2/§12。
3. **Dedup 分隔符碰撞** — `src/dedup/index.ts:13` 用 `sha1("agent:event:message")`；`agent="a",event="b:c",msg="d"` 与 `agent="a:b",event="c",msg="d"` 哈希相同 → 第二个被错误抑制。已复现。
4. **Agent 自动识别全部退化成 'agent'** — `registry.ts:detectAgentId()` 检查 env(`CLAUDE_CODE`/`CODEx`/`OPENAI_WORKERS`) 与 `process.execPath.includes('claude'/'codex')`；但 CLI 运行于 node.exe，execPath 永不匹配 → `autoDetectAndRegister()` 永远返回 'agent'。已复现。违反 v0.3 规格 §3 原则2（禁止自动发现），且 start 完全不清理 = 无界增长（Scenario B/D=3 after x3）。
  - CIM 不可用时 killStaleTrayProcesses 静默 no-op（catch{}），孤儿永不被回收。
- 根因：(1) (daemon-cli.ts:86-118) 无 killStale 调用；(2)  依赖 Get-CimInstance *TrayIcon* 子串匹配且 catch 静默；(3) daemon.ts:181 算了 tray.pid 但从未写入文件 → tray 生命周期只存在易失的 in-memory 变量，daemon 一死即失主、无记录可精确回收；(4) install-daemon.ts:57-71 只按 PID 杀旧 daemon，不杀旧 tray。
- 修复：daemon 崩溃/退出路径也要删 tray-state.json + 杀自身 tray（Job Object 绑子进程）；tray 自加 Mutex 防双开；start 也跑清理；用 PID 文件替代 CIM 子串匹配。


## 隐藏图标专项批判（18:40）
- 致命：每次 `daemon stop` 都泄漏幽灵图标。`daemon.ts:189` 用 `trayProc.kill('SIGTERM')`，Windows 上 Node 把 SIGTERM 映射成 TerminateProcess（硬杀），tray 的 `Invoke-Exit`（唯一 `Visible=$false`，TrayIcon.ps1:286）永不被调用 → 图标停在 systray 直到鼠标悬停才回收。注意：前轮"优雅 stop 不漏进程"指进程，图标是独立 shell 资源，仍泄漏。
- tray 自身信号处理全死：`trap`（:32）只 ReleaseMutex 不隐藏图标；`:307 Register-WinEvent = ${}` 无效；`:324` 在 PSConsoleHost 注册不存在的 ControlC 事件；`:317 $consoleHandler` 从未引用。Ctrl+C/关闭均不触发优雅隐藏。
- 严重/产品缺陷：图标 `Visible` 恒为 `$true`（:233），`Update-NotifyIcon` 只改颜色/badge 不切换 Visible，无"0 未读自动收起"逻辑。
- 组合灾难：前轮 crash→start 每轮 +1 孤儿 tray，每个孤儿图标永久不收起 → 溢出区堆满死图标，需逐个鼠标悬停清除。
- 修复：daemon 改发 sentinel（写 tray-state.json `exit:true` 或 signal 文件）让 tray 轮询到后优雅 Invoke-Exit；trap/循环包 try-finally 隐藏图标；按 unreadCount 切 Visible。


## 隐藏图标重检测（19:18）
- 文件再次被重写。上一轮"信号处理死代码 / 立即 SIGTERM 硬杀"已大幅修复：
  - daemon stop 改"等 3s + 删 tray-state.json + 仅最后的 SIGTERM 兜底"（daemon.ts:199-211）
  - tray 新增三种优雅退出：Ctrl+C `Add_CancelKeyPress`（:317）、`Test-TrayShouldExit`（:319-338：stopSignal / 文件消失 / daemon.pid 失联）、循环退出后 `Invoke-Exit`（:380）隐藏图标
- 但新引入决定性 bug：路径不一致。daemon 写/删 `%USERPROFILE%\.agent-attention\tray-state.json`（daemon.ts:222,79,194），tray 轮询 `%TEMP%\agent-attention-tray-state.json`（TrayIcon.ps1:224，且未用 -TrayStatePath 转发）。后果：
  (1) 文件级优雅停机信号永远到不了 tray；(2) tray 永远收不到 live 更新（只 Startup 读 state.json 一次）→ 图标计数停在启动时刻；(3) 常见 daemon stop 下 tray 三退出条件都不触发，3s 后 daemon 仍 fallback 到 `kill('SIGTERM')`。
- 已 node 实跑证明 `child.kill('SIGTERM')` 不执行子进程 cleanup handler（硬杀，mirrors daemon.ts:209）→ 绕过 tray `Invoke-Exit` → 幽灵图标在正常 stop 路径仍残留（仅延后 3s）。
- 仍成立旧项：图标 `Visible` 恒 `$true`（:233），`Update-NotifyIcon` 永不切 Visible → "0 未读自动收起"功能完全缺失（"隐藏图标"字面需求未实现）。`trap`（:32）仍只 ReleaseMutex 不隐藏图标（小缺口）。Windows 新图标默认进溢出区不变。


## UI/UX + 部署运维专项批判（20:58）
- 评分：UI/UX ~4/10，部署运维 ~3/10。
- 已确认修复（纠正上一轮 stale 结论）：tray 互斥体改 Local\；连接状态 ● 为真实字符；事件项点击调 Mark-EventRead；
  tray-state.json 路径已对齐(daemon.ts:117 传 -TrayStatePath，TrayIcon.ps1:236 接收)；install/uninstall 均 .vbs 且按 PID 杀。
- UI/UX 仍存在的确定性缺陷：
  (a) 双击仍先触发单击打开 Center：去抖方向写反，只抑制第2击，第1击已开 Center → "双击标记已读"无法不打开 Center（TrayIcon.ps1:248-278）。
  (b) 图标永远 Visible：daemon 序列化 readState 从不写 visible:false，自动隐藏功能形同虚设（TrayIcon.ps1:90 + daemon.ts:79）。
  (c) Center 用 DoEvents+Sleep 2000 轮询，state 变化即重建整个 StackPanel → 滚动位置丢失、UI 非真响应（CenterWindow.ps1:363-588）。
  (d) Toast wait:false 无可点击激活 → 点通知无任何动作（win32.ts:33）。
  (e) 左键每次启动新 powershell+WPF 窗口，延迟高、资源重。
- 部署运维确定性缺陷：
  (1) 无持久日志：daemon stdio:'ignore' 且 log() 仅 debug 才输出 → 崩溃即黑盒（P0）。
  (2) 无守护/服务/计划任务：daemon 进程崩了只能等下次登录重启（P0）。
  (3) doctor 多项硬编码假阳性（Sound ok:true 永远、Toast 仅查 platform）（P1）。
  (4) bin:uninstall 不清理 state 目录/tray.pid/lock/Toast 注册 → 卸载不干净（P1）。
  (5) install 把绝对 node.exe 路径烤进 VBS → node 升级/nvm 后自启静默失效（P1）。
  (6) 构建(tsc)不复制 .ps1；publish 包若未含 src/，全局安装后 ../src/center/*.ps1 路径断裂（P1 打包风险）。
  (7) uncaughtException 静默 exit 不记堆栈，根因不可见（P1）。
  (8) install 不校验 daemon 真启动成功（P2）。
  (9) stop 与 daemon 内部 5s 双重等待竞态，可能 SIGKILL 留 tray（P2）。
  (10) tray-state.json 读写无文件锁，偶发半写损坏（P2）。
