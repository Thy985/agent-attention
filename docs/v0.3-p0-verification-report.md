# v0.3 P0 前置条件验证报告（修正版）

**日期**: 2026-08-21
**状态**: B1–B8 全部修复，65 tests pass，build clean

---

## 历史 Bug 修复记录

| Bug | 文件 | 修复内容 |
|-----|------|---------|
| B1 | `src/notification/win32.ts:61`, `src/jump.ts:40` | `spawn(process.execPath, ['powershell',...])` → `spawn('powershell', [...])`。`process.execPath` 是 Node.exe，不能作为 PowerShell 启动器 |
| B2 | `src/notification/win32.ts` | P0 事件不再发送 snoretoast 内置音（`sound:false`），改为 PowerShell 异步播放 Asterisk+Hand 系统音；P1/P2 保持 `Notification.Default` |
| B3 | `src/registry.ts:157-169` | 删除 `detectAgentId()`/`detectAgentName()`（违反 spec §3 原则 2 的进程扫描）。`autoDetectAndRegister()` 仅读 `AGENT_ID`/`AGENT_NAME` env var，无 env 时回退到通用 `'agent'` |
| B4 | `src/dedup/index.ts:13` | `sha1("agent:event:message")` → `sha1(JSON.stringify([agent,event,message]))`，消除分隔符碰撞 |
| B5 | `src/state/AttentionState.ts:74` | `atomicWrite` 使用 `${pid}.${random4}.tmp` 替代固定 `.tmp`，消除多进程并发竞态 |
| B6 | `src/daemon-cli.ts:99,264,269` | `path.join(process.cwd(),'dist',...)` → `path.join(__dirname,'..','dist',...)`，全局安装不再失效 |
| B7 | `scripts/regression-test.ps1:25,81` | R1/R6 逻辑反转 `$after -ge $before` → `$after -le $before`（不触发 VS Code = after ≤ before） |
| B8 | `src/state/AttentionState.ts:136` | `markRead` 先检查事件是否已读再递减 unreadCount，防止重复调用的负数腐蚀 |

---

## P0-1: v0.2 真实环境验证（修正后）

| 检查项 | 状态 | 证据 |
|--------|------|------|
| Toast | ✅ PASS | `node dist/index.js completed "smoke test"` exit 0 |
| P0 Sound | ✅ SELF_COMPLETED | `node dist/index.js permission_required "smoke test"` exit 0；PowerShell spawn 路径已修复，Asterisk+Hand 系统音会异步播放 |
| Dedup | ✅ PASS | 两次相同事件第二次被抑制 (exit 0, no toast) |
| State | ✅ PASS | `~/.agent-attention/state.json` 存在，20 events 截断有效 |
| Registry | ✅ PASS | `~/.agent-attention/agents.json` 存在，agent_id 去重 |
| Daemon | ⏳ 待手动验证 | `agent-attention daemon status` 需用户机器上 daemon 正在运行 |
| Tray | ⚠️ PARTIAL | TrayIcon.ps1 PowerShell 语法问题未在此 PR 中修复 |
| CLI | ✅ PASS | `agent-notify` 和 `agent-attention` 均可执行 |

**结论**: P0-1 核心功能通过（B1 修复后 P0 声音路径真正生效）。Tray Icon 需单独修复。

---

## P0-2: Shared Registry 已存在（修正说明）

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 文件路径 | ✅ | `~/.agent-attention/agents.json` |
| 多 Agent 可写 | ✅ | `AGENT_ID=codex AGENT_NAME=Codex node dist/index.js completed test` 成功写入 codex 记录 |
| agent_id 去重 | ✅ | registry.ts 已有 agent_id 唯一性检查 |
| Schema v2 | ✅ | `{version:2, agents:[{agent_id,name,registered_at,last_seen_at,target}]}` |
| **⚠️ 旧报告伪证** | ❌ 已更正 | 原报告 `AGENT_NAME=codex node ...` 是错误的——代码只认 `AGENT_ID`，`AGENT_NAME` 单独无法设置 agent_id |

**结论**: P0-2 通过，但原验证步骤有误导（B14 已更正）。

---

## P0-3: Shared State 已存在

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 文件路径 | ✅ | `~/.agent-attention/state.json` |
| 含 agent_id 字段 | ✅ | StateEvent.agent_id 已存在 |
| 多 Agent 可写 | ✅ | 测试覆盖（多 agent_id 分别 recordEvent） |
| 20 条截断 | ✅ PASS | 已有测试覆盖 |
| 原子写 | ✅ PASS | `.tmp` + renameSync，已改为随机 tmp 名防并发（B5） |

**结论**: P0-3 通过。

---

## P0-4: Agent Shell/CLI 能力

同原报告，Agent 具备 Shell 执行/文件读写/进程管理/npm 能力。

---

## 测试矩阵

```
Test Suites: 8 passed, 8 total
Tests:       65 passed, 65 total
Snapshots:   0 total
```

新增测试：
- `tests/dedup.test.ts`: B4 分隔符碰撞回归测试
- `tests/state.test.ts`: B8 markRead 幂等性 + 不存在 eventId 处理
- `tests/registry.test.ts`: B3 autoDetectAndRegister 环境变量优先/无 env 回退 'agent'
- `tests/cli-invariant.test.ts`: B1 源文件不变量（assert 'powershell' literal, not process.execPath）
- `tests/jump.test.ts`: B1 源文件不变量

---

## 构建状态

```
✓ tsc build clean (no errors)
✓ dist/teammind/ 陈旧产物已清理 (B13)
```

---

## 已知剩余问题

1. **TrayIcon.ps1 语法问题** — P0-1 PARTIAL，单独修复。
2. **v0.3 spec §9/§12.2 S6 DoD** — 原标记 Target Jump ✅，实现曾为死代码（B1 已修复）。
3. **v0.3 spec §3 原则 2 vs §62 defer** — 原 autoDetectAndRegister 明做进程扫描，与原则 2 冲突（B3 已移除）。
4. **B15 spec 自相矛盾** — 已同步修正代码行为以符合 spec 原则 2。

---

## 下一步

1. 修复 TrayIcon.ps1 语法问题（P0-1 完整通过）
2. 按 v0.3 spec 实施 Phase 1–4（Onboarding / Registry v2 / Center / Jump）

---

## Center/Tray/Install 灾难修复（2026-08-21）

详见 [docs/center-tray-fix-report.md](./center-tray-fix-report.md)。

| 编号 | 问题 | 状态 |
|------|------|------|
| ① | Center 窗口永远打不开（Mutex 反斜杠） | ✅ |
| ② | install/uninstall .vbs/.lnk 不一致 | ✅ |
| ③ | uninstall 杀光所有 node.exe | ✅ |
| ④ | install 裸 node + 无 PID 管理 | ✅ |
| ⑤ | Tray 缺消息循环，点击不响应 | ✅ |
| ⑥ | 点击弹黑框 PowerShell | ✅ |
| ⑦ | exit 不真正退出 | ✅ |
| ⑧ | 中文 UI 乱码（无 BOM） | ✅ |
| ⑨ | MiniPanel 死代码 + Pester 误测 | ✅ |
| ⑩ | Center 一次性快照 | ⚠️ 已知限制 |
| ⑪ | mark-all-read 依赖 node PATH | ✅ |
